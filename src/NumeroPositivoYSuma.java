/*import java.util.Scanner;

public class NumeroPositivoYSuma {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        int sum = 0;

        while (true) {
            System.out.print("Ingresa un número positivo (ingresa un número negativo para terminar): ");
            int userInput = scanner.nextInt();

            if (userInput < 0) {
                break;
            }

            sum += userInput;
        }

        System.out.println("La suma de los números positivos ingresados es: " + sum);
        scanner.close();
    }
}
*/